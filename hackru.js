function scaleup(obj, size){
    obj.style.fontSize = size;
}

function scaleup2(obj, size){
    obj.style.fontSize = size;
    obj.style.transform = "scale(1.05)";
}

function scaledown(obj)
{
    obj.style.fontSize = "30px"
    obj.style.transform= "scale(1)";
}